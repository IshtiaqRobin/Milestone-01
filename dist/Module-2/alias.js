"use strict";
// alis 
const crush1 = {
    name: "Suborna",
    age: 17,
    profession: "Lover",
    address: "Kanaipur",
};
const crush2 = {
    name: "Riya Islam",
    profession: "Nursing",
    address: "BD"
};
const isCrushMarried = false;
const courseName = 'Next Level Web Development';
const calculate = (number1, // 10
number2, // 20 
operation // (x, y) => x + y) (10, 20) => 10 + 20;a
) => {
    return operation(number1, number2);
};
calculate(10, 20, (x, y) => x + y);
calculate(10, 20, (x, y) => x - y);
calculate(10, 20, (x, y) => x * y);
calculate(10, 20, (x, y) => x / y);
